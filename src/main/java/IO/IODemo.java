package IO;

public class IODemo {
    /*
    IO操作类在包java.io下，大概有将近80个类，这些类可以分为如下四组：
    基于字节操作的IO接口：InputStream和OutputStream
    基于字符操作的IO接口：Reader和Writer
    基于磁盘操作的IO接口：File
    基于网络操作的IO接口：Socket

    IO流的分类
    根据[处理数据类型]的不同分为：字符流和字节流。
    根据[数据流向]不同分为：输入流(InputStream)和输出流(OutputStream)。
     */
}
